import path from 'path';

// paths
export const JSON_DIRECTORY = path.join(__dirname, '../json');
export const HISTORY_DIRECTORY = path.join(__dirname, '../../../seats_history');
export const BACKUP_DIRECTORY = path.join(__dirname, '../../../seats_backup');
export const FULL_SEATS = 'seats(full).json';
export const CURRENT_SEATS = 'seats.json';
export const LAST_WEEK_SEATS = 'seats_last_week.json';
